{
  "name": "epub-reader-demo",
  "version": "1.0.0",
  "description": "Demo version of epub reader with basic functionality",
  "scripts": {
    "setup": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "start": "npm run dev",
    "test-server": "cd server && npm test",
    "test-client": "cd client && npm test",
    "install-server-deps": "cd server && npm install",
    "add-ui-deps": "cd client && npx shadcn@latest add button card input label"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}